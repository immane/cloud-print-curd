# Subprojects

This directory contains the implementation subprojects for Cloud Print.

- `src/backend`: FastAPI backend service, database migrations, worker, and tests.
- `src/frontend-wechat`: Vue-based WeChat mini-app style frontend (H5 simulation).

Each subproject includes its own `README.md` with setup and test instructions.
